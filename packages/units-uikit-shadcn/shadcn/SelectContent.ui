div (className=@('z-50 min-w-[8rem] overflow-hidden rounded-md border border-border bg-popover p-1 text-popover-foreground shadow' + (props.className ? ' ' + props.className : ''))) {
  #slot (default)
}
