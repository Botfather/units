button (
  className=@('inline-flex items-center rounded-sm px-3 py-1.5 text-sm font-medium hover:bg-accent' + (props.className ? ' ' + props.className : '')),
  type:'button'
) {
  #slot (default)
}
