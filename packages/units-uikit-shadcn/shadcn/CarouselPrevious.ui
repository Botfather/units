button (
  className=@('absolute left-2 top-1/2 -translate-y-1/2 inline-flex h-8 w-8 items-center justify-center rounded-full border border-border bg-background shadow' + (props.className ? ' ' + props.className : '')),
  type:'button'
) {
  #slot (default)
}
