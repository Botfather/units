label (className=@('inline-flex items-center gap-2 text-sm' + (props.className ? ' ' + props.className : ''))) {
  input (
    type:'radio',
    className:'peer h-4 w-4 shrink-0 rounded-full border border-input text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50',
    name=@props.name,
    value=@props.value,
    checked?=@props.checked,
    disabled?=@props.disabled,
    !change { props.onCheckedChange && props.onCheckedChange(@event) }
  )
  #slot (default)
}
