div (className=@('min-w-[12rem] overflow-hidden rounded-md border border-border bg-popover p-1 text-popover-foreground shadow' + (props.className ? ' ' + props.className : ''))) {
  #slot (default)
}
