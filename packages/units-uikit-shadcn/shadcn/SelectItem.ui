div (className=@('relative flex w-full cursor-default select-none items-center rounded-sm px-2 py-1.5 text-sm outline-none hover:bg-accent' + (props.className ? ' ' + props.className : ''))) {
  #slot (default)
}
