button (
  className=@('flex w-full items-center justify-between py-4 font-medium transition-all hover:underline' + (props.className ? ' ' + props.className : '')),
  type:'button'
) {
  #slot (default)
}
