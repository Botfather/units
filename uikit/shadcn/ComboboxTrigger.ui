button (
  className=@('inline-flex w-full items-center justify-between rounded-md border border-input bg-background px-3 py-2 text-sm' + (props.className ? ' ' + props.className : '')),
  type:'button'
) {
  #slot (default)
}
