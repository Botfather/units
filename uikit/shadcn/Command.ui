div (className=@('flex h-full w-full flex-col overflow-hidden rounded-md border border-border bg-popover text-popover-foreground' + (props.className ? ' ' + props.className : ''))) {
  #slot (default)
}
