textarea (
  className=@('flex min-h-[80px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50' + (props.className ? ' ' + props.className : '')),
  value=@props.value,
  placeholder=@props.placeholder,
  disabled?=@props.disabled,
  !input { props.onInput && props.onInput(@event) }
) {
  #slot (default)
}
