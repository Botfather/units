div (className=@('group pointer-events-auto relative flex w-full items-start gap-2 overflow-hidden rounded-md border border-border bg-background p-4 shadow' + (props.className ? ' ' + props.className : ''))) {
  #slot (default)
}
