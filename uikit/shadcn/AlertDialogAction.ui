button (
  className=@('inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90' + (props.className ? ' ' + props.className : '')),
  type:'button'
) {
  #slot (default)
}
