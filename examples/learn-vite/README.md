# Learn Units

A minimal reference site built entirely with the Units DSL. The page explains the grammar, compares common React/JSX patterns with their Units equivalents, and includes live interactive demos (counter + todo list) — all rendered from a single `.ui` file.

## Run

```bash
pnpm install
pnpm dev
```
