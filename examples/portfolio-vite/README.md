# Units Portfolio (Vite)

## Install
```
npm install
```

## Run
```
npm run dev
```

## What it shows
- Portfolio header rendered from `src/portfolio.ui`
- Units runtime with scoped profile data (`name`, `role`, `location`, `initials`)
- ShadCN-style Units components (`Avatar`, `AvatarFallback`, `Button`)
